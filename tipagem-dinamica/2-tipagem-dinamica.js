// Na tipagem dinamica não há necessidade de declarar o tipo da variavel
// Assim que atribuimos o valor a ela, o compilador já determina o tipo por inferência
var minhaVariavel = 30;

minhaVariavel = 'texto';

console.log(minhaVariavel);